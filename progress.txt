# Ralph Progress Log
Started: Mon Jan 19 16:47:53 PST 2026

## Codebase Patterns
- CLI uses argparse with subparsers for subcommands (scrape, analyze, rankings, status)
- Each command has a cmd_<name> function handler in main.py that imports and calls module functions
- Module functions are imported lazily inside command handlers to avoid import errors when modules are incomplete
- Database: Use `from db import get_connection` to get SQLite connections, DB file is `mr_books.db`
- All database tables use CREATE TABLE IF NOT EXISTS for idempotent initialization
- Scraper: Use `cloudscraper` instead of `requests` to bypass Cloudflare protection on marginalrevolution.com
- Scraper: Article entries are in `<article>` tags, title links have `rel='bookmark'` attribute
- Scraper: Pagination can be found via link text "Next" or by looking for `/page/N` in href
- Scraper: Post content is in `div.entry-content` class; use fetch_with_retry() for resilient requests
- Analyzer: Use lazy imports inside functions to avoid circular imports between db.py and analyzer.py
- Analyzer: spaCy model 'en_core_web_sm' is required; code auto-downloads if missing
- Deduplication: Use `find_or_create_book()` from db.py instead of get_book_by_title/insert_book for fuzzy matching
- Deduplication: `normalize_title()` strips articles ("the", "a", "an") and punctuation for comparison
- Deduplication: Uses rapidfuzz library with token_sort_ratio for 85% similarity threshold
- Sentiment: Use VADER via `vaderSentiment.vaderSentiment.SentimentIntensityAnalyzer`; compound score is -1 to 1
- Sentiment: Scores are stored at both mention level (book_mentions.sentiment_score) and book level (books.sentiment_score)
- Sentiment (Ollama): Use analyze_sentiment_ollama() with --use-ollama flag; calls localhost:11434, handles interview transcripts
- Interview Detection: is_interview_or_transcript() detects speaker tags; extract_tyler_opinion() filters to Tyler's sections only
- Pagination: Use is_valid_pagination_url() to validate next page links; filters out external domains and invalid URLs
- Parallel Scraping: Use ThreadPoolExecutor + RateLimiter class for concurrent fetching; collect results, write to DB sequentially
- Caching: posts.books_extracted_at tracks extraction completion; use get_posts_for_extraction() for incremental processing
- Fail-fast: Use check_ollama_available() at start when --use-ollama to avoid wasting time if Ollama unavailable
- Schema Migration: Use PRAGMA table_info() + ALTER TABLE ADD COLUMN for backwards-compatible migrations
- Full-context LLM: Use analyze_post_with_llm() with --full-context flag for single LLM call per post extracting books + sentiment
- Full-context LLM: Returns JSON array; use regex to extract [...] pattern when LLM adds extra text
- Full-context LLM: Use insert_book_mention_with_sentiment() to store sentiment directly with mention
- Bradley-Terry: Use `python main.py rank --comparisons N` for pairwise ranking; uses choix library
- Bradley-Terry: comparisons table stores pairwise results; bt_score columns on book_mentions and books
- Bradley-Terry: Use sample_pairs() for random sampling; compare_pairs_parallel() for parallel LLM calls
- Bradley-Terry: rankings command auto-detects and uses bt_score when available via has_bt_scores()
- Bradley-Terry (Adaptive): Use --adaptive flag for uncertainty-based sampling; refits model every 1000 comparisons
- Bradley-Terry (Adaptive): sample_pairs_adaptive() uses three-tier strategy: coverage → uncertainty → random
- Bradley-Terry (Top-k): Use --top-k N flag to focus on identifying top N items; refits every 500 comparisons
- Bradley-Terry (Top-k): sample_pairs_topk() uses 70% top-tier, 20% top-vs-random, 10% random; prioritizes "bubble" items
- Bradley-Terry (Top-k): Early stopping after 3 consecutive stable refits (100% stability in top-k set)
- Debug Mode: Use --debug with rank command; compare_pair_with_llm() returns dict when debug=True for detailed logging
- Debug Mode: Use --debug-log FILE to write debug output to file instead of terminal
- Extract-Only Mode: Use --extract-only with analyze to extract book titles without sentiment (stores full post as context_text)
- Extract-Only Mode: extract_books_only() returns JSON array of title strings; use insert_book_mention() without sentiment
- Extract-Only Mode: clear_books_data() and reset_extraction_cache() clear old data for fresh start
- Parallel Extraction: Use --workers N flag with analyze command; extract_books_from_posts_llm() uses ThreadPoolExecutor + RateLimiter
- Parallel Extraction: Batch DB writes (50 at a time) to avoid SQLite concurrency issues; as_completed() processes results as they arrive
- JSON Cleanup: Use clean_markdown_from_json() to strip * and _ from outside quoted strings before JSON parsing
- JSON Cleanup: State machine approach tracks in_quotes flag to distinguish inside/outside string content
- Fallback Extraction: Use extract_titles_from_malformed_response() when JSON parsing fails; extracts from quoted strings and lists
- LLM Prompts: Explicitly tell LLM "Return ONLY valid JSON. Do NOT use markdown formatting like * or _" to reduce parsing issues
---

## 2026-01-19 - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented project structure and requirements.txt
- Files created:
  - requirements.txt: dependencies (requests, beautifulsoup4, vaderSentiment, spacy, lxml)
  - main.py: CLI with argparse subcommands (scrape, analyze, rankings, status)
  - db.py: stub module for database operations
  - scraper.py: stub module for web scraping
  - analyzer.py: stub module for NLP/sentiment analysis
- **Learnings for future iterations:**
  - The CLI uses lazy imports in command handlers to avoid import errors from incomplete modules
  - `python main.py --help` runs successfully, confirming the basic structure works
  - Each subcommand has its own set_defaults(func=cmd_X) for routing
---

## 2026-01-19 - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented SQLite database schema in db.py
- Files modified:
  - db.py: Added init_db() function with all required tables
- Tables created:
  - posts: id, url (unique), title, date_published, content_html, content_text, scraped_at
  - books: id, title, author, sentiment_score, genre, created_at
  - book_mentions: id, book_id, post_id, context_text, sentiment_score, UNIQUE(book_id, post_id)
  - scrape_progress: id, page_number, completed_at
- **Learnings for future iterations:**
  - DB_PATH is set to be in the same directory as db.py using os.path.dirname(os.path.abspath(__file__))
  - get_connection() helper function available for getting database connections
  - All tables use CREATE TABLE IF NOT EXISTS for idempotent initialization
  - book_mentions table has foreign keys to books and posts tables
---

## 2026-01-19 - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented category listing scraper in scraper.py
- Files modified:
  - scraper.py: Implemented scrape_category_listing() function
  - db.py: Added insert_post(), record_page_scraped(), get_last_scraped_page() functions
  - requirements.txt: Added cloudscraper dependency
- Features:
  - Uses cloudscraper to bypass Cloudflare protection
  - Extracts post URL, title, and date from article tags
  - Handles pagination by finding "Next" link or /page/N pattern
  - Implements 1-2 second random delay between requests
  - Supports resume from last scraped page using scrape_progress table
  - Logs progress showing page number and posts found
- **Learnings for future iterations:**
  - marginalrevolution.com has Cloudflare protection, standard requests returns 403
  - Use `cloudscraper.create_scraper()` to handle the JavaScript challenge automatically
  - Post titles are in `<a rel="bookmark">` tags within `<article>` elements
  - Dates are in `<time>` tags with `datetime` attribute
  - Site has ~279 pages in the Books category
---

## 2026-01-19 - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented individual post content scraper in scraper.py
- Files modified:
  - scraper.py: Added scrape_post_content() and fetch_with_retry() functions
  - db.py: Added get_posts_without_content(), get_total_post_count(), update_post_content() functions
- Features:
  - Fetches posts where content_text is NULL
  - Extracts main post body from `div.entry-content` (WordPress pattern)
  - Removes comments and sidebar elements from content
  - Stores both HTML and cleaned text
  - Retry logic with exponential backoff (2, 4, 8 seconds)
  - 1-2 second random delay between requests
  - Progress logging showing current/total posts
- **Learnings for future iterations:**
  - Post content is in `div.entry-content` class (standard WordPress)
  - Some pages like "Marginal Revolution University" don't have entry-content (promotional pages)
  - fetch_with_retry() can be reused for other network requests that need resilience
  - Pagination on listing pages sometimes picks up external links (e.g., Amazon) - the scraper gracefully handles this
---

## 2026-01-19 - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented status command in db.py
- Files modified:
  - db.py: Implemented get_scrape_status() function
- Features:
  - Returns total posts in DB, posts with content scraped, last scrape timestamp
  - Shows number of pages scraped, posts found, posts with content
  - Displays content coverage percentage
  - Formatted terminal output with aligned columns
  - Returns dict for programmatic use as well as printing
- **Learnings for future iterations:**
  - Status function both prints AND returns data for flexibility
  - Uses MAX(scraped_at) from posts table for last content scrape time
  - Uses MAX(completed_at) from scrape_progress table for last listing scrape time
---

## 2026-01-19 - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented book title extraction using NLP in analyzer.py
- Files modified:
  - analyzer.py: Implemented extract_books_from_post(), analyze_all_posts(), and helper functions
  - db.py: Added get_posts_with_content(), insert_book(), get_book_by_title(), insert_book_mention(), get_all_books()
- Features:
  - Three extraction methods: italicized text (<em>/<i> tags), spaCy NER (WORK_OF_ART entities), regex patterns
  - Context extraction: captures surrounding text (~300 chars) for each book mention
  - Title cleaning: filters out URLs, short/long text, common phrases
  - Lazy loading of spaCy model with auto-download if missing
  - Progress logging showing posts processed and mentions found
- **Learnings for future iterations:**
  - spaCy's WORK_OF_ART entity can produce false positives (e.g., "everyone" detected as work)
  - Use lazy imports (`from db import ...` inside functions) to avoid circular import issues
  - Italicized text is the most reliable source of book titles in blog posts
  - Limit NLP processing to first 100k chars to avoid memory issues with large texts
  - The analyzer module now has dependencies: re, BeautifulSoup, spacy
---

## 2026-01-19 - US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented book deduplication using fuzzy string matching in db.py
- Files modified:
  - db.py: Added normalize_title() and find_or_create_book() functions
  - analyzer.py: Updated extract_books_from_post() to use find_or_create_book()
  - requirements.txt: Added rapidfuzz dependency
- Features:
  - normalize_title(): strips leading articles (the, a, an), removes punctuation, lowercases
  - find_or_create_book(): uses rapidfuzz token_sort_ratio for fuzzy matching
  - 85% similarity threshold for considering titles as the same book
  - Correctly deduplicates "The Great Gatsby" vs "Great Gatsby" (100% match after normalization)
- **Learnings for future iterations:**
  - rapidfuzz is faster than python-Levenshtein and fuzzywuzzy
  - token_sort_ratio handles word order differences (e.g., "Tale Two Cities" vs "Two Cities Tale")
  - When comparing many titles, normalizing first reduces redundant computation
  - The find_or_create_book() function should be used everywhere books are created for consistency
---

## 2026-01-19 - US-008
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented sentiment analysis for book mentions in analyzer.py
- Files modified:
  - analyzer.py: Implemented analyze_sentiment(), analyze_book_mentions() functions; updated analyze_all_posts()
  - db.py: Added get_unanalyzed_mentions(), update_mention_sentiment(), update_book_sentiment() functions
- Features:
  - analyze_sentiment(): uses VADER SentimentIntensityAnalyzer to get compound score (-1 to 1)
  - analyze_book_mentions(): processes all unanalyzed mentions, updates individual and aggregated scores
  - Skips already-analyzed mentions for idempotent re-runs
  - Aggregates average sentiment across all mentions for each book in books.sentiment_score
  - Progress logging showing mention analysis count
- **Learnings for future iterations:**
  - VADER returns a dict with 'neg', 'neu', 'pos', and 'compound' scores; compound is the normalized overall score
  - Use lazy imports for vaderSentiment inside analyze_sentiment() to match existing patterns
  - The analyze command now runs both book extraction AND sentiment analysis in sequence
  - Sentiment scores are stored in both book_mentions and books tables for different query needs
---

## 2026-01-19 - US-009
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented ranked book list display in db.py
- Files modified:
  - db.py: Implemented get_ranked_books() function
- Features:
  - Returns books sorted by sentiment_score descending
  - Includes all post URLs where each book is mentioned (fetched via JOIN)
  - Formatted terminal output: rank, title, score, post links
  - Supports --top N flag to limit results
  - Supports --genre filter (ready for US-010)
  - Shows count of mentions and limits URL display to 3 per book for readability
- **Learnings for future iterations:**
  - The rankings command already had --top and --genre flags set up in main.py
  - get_ranked_books() both prints AND returns data for flexibility (matches pattern from get_scrape_status())
  - Use parameterized queries with dynamic SQL building for optional filters
  - Limiting URL display to 3 per book prevents terminal overflow while still showing data availability
---

## 2026-01-19 - US-010
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented genre categorization for books in analyzer.py
- Files modified:
  - analyzer.py: Implemented categorize_book() and categorize_all_books() functions
  - db.py: Added get_books_without_genre(), get_book_contexts(), update_book_genre() functions
- Features:
  - categorize_book(): uses keyword matching on content to assign genres
  - Supports genres: history, economics, fiction, biography, science, philosophy, other
  - Combines both context_text and full post content for better keyword matching
  - Requires minimum of 2 keyword matches to assign a specific genre (otherwise 'other')
  - Integrated into analyze_all_posts() flow after sentiment analysis
  - Rankings --genre filter already worked (was implemented in US-009)
- **Learnings for future iterations:**
  - The --genre flag and genre filter in get_ranked_books() were already set up in US-009
  - Genre keywords should be ordered by specificity for better matching
  - Combining book mention context_text with full post content provides richer data for categorization
  - A threshold (min 2 keyword matches) helps avoid false categorizations
---

## 2026-01-19 - US-011
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Fixed pagination bug for pages 9+ in scraper.py
- Files modified:
  - scraper.py: Added is_valid_pagination_url() function and refactored pagination logic
- Features:
  - is_valid_pagination_url(): validates URLs match the expected Books category pattern
  - Filters out external domains (Amazon, etc.), anchor links (#), and javascript: links
  - Validates that URLs point to the correct page number
  - Multiple strategies for finding next page with URL construction as fallback
  - Successfully scrapes pages 9, 10, and beyond (tested to page 49+)
- **Learnings for future iterations:**
  - Pagination in WordPress sites can include unrelated links (Amazon affiliate, etc.)
  - Always validate that pagination URLs match the expected domain/pattern before following
  - When pagination element hides some page numbers (ellipsis), the link may still work by constructing URL directly
  - Adding VALID_PAGINATION_PATTERN constant helps centralize the URL validation logic
---

## 2026-01-19 - US-012
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented parallel post content scraping in scraper.py
- Files modified:
  - scraper.py: Added RateLimiter class, _fetch_single_post() helper, modified scrape_post_content()
  - main.py: Added --workers flag to scrape command
- Features:
  - RateLimiter class: thread-safe rate limiting across workers (~5 requests/second)
  - ThreadPoolExecutor for parallel fetching with configurable worker count
  - Batch processing in groups of 50 posts to manage memory
  - Sequential DB writes after batch completion (avoids SQLite concurrent write issues)
  - Progress logging shows current/total with success/failure status
  - Preserved retry logic via fetch_with_retry() in worker threads
- **Learnings for future iterations:**
  - SQLite doesn't handle concurrent writes well - always collect results and write sequentially
  - RateLimiter with a lock ensures thread-safe timing across all workers
  - Processing in batches (50) helps manage memory while still achieving parallelism
  - The `as_completed()` function is key for processing results as they arrive
---

## 2026-01-19 - US-013
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented Ollama LLM sentiment analysis in analyzer.py
- Files modified:
  - analyzer.py: Added analyze_sentiment_ollama(), is_interview_or_transcript(), extract_tyler_opinion() functions
  - main.py: Added --use-ollama and --model flags to analyze command
  - db.py: Updated get_unanalyzed_mentions() to return book title for interview extraction
- Features:
  - analyze_sentiment_ollama(): Calls Ollama API at localhost:11434
  - is_interview_or_transcript(): Detects speaker tags like 'TYLER:', 'GUEST:', 'TC:', etc.
  - extract_tyler_opinion(): Extracts only Tyler's sections from transcript text
  - If Tyler doesn't mention a book in his sections, returns None (skip) instead of guest sentiment
  - Normalizes LLM 1-10 scores to -1 to 1 scale: (score - 5.5) / 4.5
  - Graceful error handling with helpful messages when Ollama is not available
  - --model flag allows choosing any Ollama model (default: llama3.2:3b)
- **Learnings for future iterations:**
  - Interview/transcript posts use speaker tags at line starts: 'TYLER COWEN:', 'GUEST:', 'TC:', 'Q:', 'A:'
  - Need at least 2 speaker-tagged lines to consider text as interview
  - When extracting Tyler's opinion, search for book title mention in his sections
  - Ollama API at localhost:11434/api/generate accepts JSON with model, prompt, stream, options
  - Low temperature (0.1) helps get consistent numeric ratings from LLM
  - Use urllib instead of requests to avoid adding new dependency
---

## 2026-01-19 - US-014
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented book extraction caching in db.py and analyzer.py
- Files modified:
  - db.py: Added books_extracted_at column, get_posts_for_extraction(), mark_post_books_extracted()
  - analyzer.py: Added check_ollama_available(), modified analyze_all_posts() to use caching
  - main.py: Added --reextract flag to analyze command
- Features:
  - books_extracted_at: Nullable timestamp column marking when post was processed
  - get_posts_for_extraction(reextract=False): Returns posts needing extraction (or all with reextract=True)
  - mark_post_books_extracted(): Sets books_extracted_at to current time
  - check_ollama_available(): Validates Ollama connection and model availability BEFORE starting work
  - --reextract flag: Forces re-extraction from all posts, ignoring cache
  - Skips book extraction for already-processed posts, still runs sentiment analysis on unanalyzed mentions
- **Learnings for future iterations:**
  - Use PRAGMA table_info() to check if column exists before ALTER TABLE ADD COLUMN
  - Fail-fast pattern: Check external dependencies (Ollama) at start, not after expensive work
  - Ollama /api/tags endpoint lists available models; check before attempting generation
  - When caching, provide both "skip if cached" and "force refresh" options (--reextract)
  - Even when skipping extraction, continue to sentiment analysis/categorization phases
---

## 2026-01-19 - US-015
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented full-context LLM analysis for book extraction and sentiment
- Files modified:
  - analyzer.py: Added analyze_post_with_llm(), analyze_posts_full_context() functions
  - db.py: Added insert_book_mention_with_sentiment() function
  - main.py: Added --full-context flag to analyze command
- Features:
  - analyze_post_with_llm(): Single LLM call per post that returns JSON array of books with sentiment
  - Takes full post content (up to 8000 chars) instead of 300-char snippets
  - Prompt instructs LLM to find ALL books and rate Tyler's sentiment 1-10
  - Different prompts for regular posts vs interview/transcript posts
  - For interviews, only extracts Tyler's opinions, not guests'
  - Returns normalized sentiment score (-1 to 1) matching existing scale
  - JSON parsing handles LLM adding extra text around the JSON array
  - Graceful error handling: logs warning and skips post on JSON parse failures
  - Uses find_or_create_book() for deduplication across posts
  - Stores LLM reasoning as context_text for reference
  - --full-context flag enables this mode (keeps old approach as fallback)
- **Learnings for future iterations:**
  - LLMs often add extra text around JSON output; use regex to extract [...] pattern
  - Truncate long posts (>8000 chars) to avoid token limits
  - Different prompts for interview vs regular posts improves accuracy
  - Score normalization: (score - 5.5) / 4.5 converts 1-10 to -1 to 1
  - When inserting mentions with sentiment, use insert_book_mention_with_sentiment() to set score directly
  - Full-context mode skips separate sentiment analysis phase since LLM already scored books
---

## 2026-01-20 - US-016
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented Bradley-Terry pairwise ranking for book sentiment
- Files modified:
  - requirements.txt: Added choix library for Bradley-Terry model fitting
  - db.py: Added comparisons table, bt_score columns, and db functions for BT operations
  - analyzer.py: Added sample_pairs(), compare_pair_with_llm(), compare_pairs_parallel(), fit_bradley_terry(), run_pairwise_ranking()
  - main.py: Added 'rank' command with --comparisons, --model, --workers flags; updated rankings to use bt_score when available
- Features:
  - comparisons table stores pairwise comparison results (mention_a_id, mention_b_id, winner_id)
  - bt_score columns on book_mentions and books tables for Bradley-Terry strength scores
  - sample_pairs(): random sampling of mention pairs for comparison
  - compare_pair_with_llm(): asks LLM "which review is more positive?" returning A, B, or TIE
  - compare_pairs_parallel(): ThreadPoolExecutor with RateLimiter for parallel LLM calls
  - fit_bradley_terry(): uses choix.opt_pairwise() for maximum likelihood estimation
  - Aggregates mention-level BT scores to book-level using weighted average (weight = comparison count)
  - Rankings command auto-detects and uses bt_score when available
  - Incremental: can stop and resume, adding more comparisons improves rankings
- **Learnings for future iterations:**
  - choix library expects (winner_idx, loser_idx) tuples; convert from (a, b, winner) format
  - Use opt_pairwise() with alpha regularization to handle sparse comparison graphs
  - Ties are excluded from BT fitting (could add tie handling with proper Bradley-Terry extensions)
  - Rate limiting across workers: RateLimiter with lock ensures thread-safe timing
  - Batch db operations (insert_comparisons_batch, update_mention_bt_scores_batch) for efficiency
  - LLM comparison prompt should be explicit about what to compare (review positivity, not book quality)
---

## 2026-01-20 - US-017
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented adaptive/uncertainty sampling for Bradley-Terry comparisons
- Files modified:
  - analyzer.py: Added sample_pairs_adaptive(), _sample_uncertain_pair(), compute_uncertainty_metric(), _run_adaptive_ranking()
  - db.py: Added get_mention_comparison_counts(), get_mentions_with_bt_scores()
  - main.py: Added --adaptive flag to rank command
- Features:
  - sample_pairs_adaptive(): Three-tier sampling strategy: coverage (60%), uncertainty (32%), random fallback (8%)
  - Coverage-focused: Prioritizes items with < 5 comparisons to ensure minimum coverage
  - Uncertainty-focused: Uses exponential weighting e^(-5*diff) to favor pairs with similar BT scores
  - _sample_uncertain_pair(): Samples 50 candidates and uses weighted random selection
  - compute_uncertainty_metric(): Tracks total uncertainty, avg gap between ranks, coverage percentage
  - _run_adaptive_ranking(): Refits BT model every 1000 comparisons, updates uncertainty estimates
  - Logs uncertainty reduction per batch and overall efficiency metrics
  - Falls back to random sampling for new items or when uncertainty sampling fails
- **Learnings for future iterations:**
  - Exponential weighting e^(-5*diff) works well for uncertainty sampling - high sensitivity to small differences
  - Three-tier strategy balances coverage vs uncertainty: prioritize coverage early, then focus on close calls
  - Sampling 50 candidates for uncertainty pairs is a good balance between quality and speed
  - Periodic refitting (every 1000 comparisons) keeps uncertainty estimates fresh without too much overhead
  - Tracking uncertainty reduction per 1000 comparisons gives good efficiency metrics
---

## 2026-01-20 - US-018
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented top-k focused sampling for faster convergence on top rankings
- Files modified:
  - analyzer.py: Added sample_pairs_topk(), compute_topk_stability(), _run_topk_ranking(), _get_topk_set()
  - main.py: Added --top-k flag to rank command, updated cmd_rank() to pass top_k parameter
- Features:
  - sample_pairs_topk(): Three-tier sampling strategy focusing on identifying top-k items
    - 70% top-tier comparisons (comparing items in top 2*k)
    - 60% of top-tier focused on "bubble" items around the cutoff (k-20 to k+20)
    - 20% top vs random (verify top items beat average items)
    - 10% pure random (discover dark horses)
  - compute_topk_stability(): Tracks how much the top-k set changed since last refit
  - _run_topk_ranking(): Refits BT model every 500 comparisons (vs 1000 for adaptive)
  - Early stopping: Stops if top-k is 100% stable for 3 consecutive refits
  - Logs top-k stability history and convergence progress
  - --top-k flag added to 'python main.py rank' command
- **Learnings for future iterations:**
  - "Bubble" items (around the k cutoff) are the most important to compare - they determine who's in/out
  - 70/20/10 split gives good balance: most effort on top items, some verification, some exploration
  - Refitting every 500 (not 1000) makes sense for top-k since the focus is narrower
  - Early stopping on stability can save significant compute when top-k converges
  - _get_topk_set() helper is useful for comparing sets across iterations
---
## 2026-01-20 - US-019
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented debug mode for LLM comparison logging in rank command
- Files modified:
  - main.py: Added --debug and --debug-log flags to rank command, passed to run_pairwise_ranking()
  - analyzer.py: Modified compare_pair_with_llm(), compare_pairs_parallel(), run_pairwise_ranking(), _run_adaptive_ranking(), _run_topk_ranking()
- Features:
  - --debug flag enables detailed logging of each LLM comparison
  - --debug-log FILE writes debug output to file instead of terminal
  - compare_pair_with_llm() returns debug info dict when debug=True: title_a, title_b, prompt, raw_response, parsed_result, error
  - Debug output format includes:
    - Comparison number
    - Book A and Book B titles
    - Full prompt sent to LLM
    - Raw LLM response before parsing
    - Parsed result (A, B, or TIE) and any parsing issues
    - Separator lines for readability
  - Shows tip about using --workers 1 when debug mode is enabled
  - Supports file output mode for reviewing logs without terminal spam
  - All three ranking modes (random, adaptive, top-k) support debug mode
- **Learnings for future iterations:**
  - When adding debug mode, pass debug params through the entire call chain
  - Debug dict return vs tuple return requires conditional handling in caller
  - File mode debug logging needs proper cleanup with try/finally to close file handle
  - Thread-safe counter with lock needed for comparison numbering in parallel mode
  - Showing tips/recommendations (like --workers 1) helps user experience
---

## 2026-01-20 - US-020
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented extraction-only mode that stores full post text instead of LLM reasoning
- Files modified:
  - analyzer.py: Added extract_books_only(), extract_books_from_posts_llm() functions; modified analyze_all_posts()
  - db.py: Added clear_books_data(), reset_extraction_cache() functions
  - main.py: Added --extract-only flag to analyze command
- Features:
  - extract_books_only(): LLM prompt asks for JSON array of book title strings (no sentiment/reasoning)
  - Includes both post.title AND post.content_text in LLM prompt (book might be in title)
  - Stores full post context (title + '\n\n' + content_text) as context_text for all books
  - clear_books_data(): Clears comparisons, book_mentions, books tables for fresh start
  - reset_extraction_cache(): Resets posts.books_extracted_at to NULL for re-extraction
  - Interview/transcript detection still applies (only extracts books Tyler mentions)
  - No sentiment_score stored during extraction - that comes from Bradley-Terry later
  - --extract-only flag: python main.py analyze --use-ollama --extract-only
- **Learnings for future iterations:**
  - Separation of concerns: extraction finds titles, Bradley-Terry handles ranking
  - Store FULL post content as context_text so comparisons have real text to analyze
  - Multiple books from same post have identical context_text - that's fine, LLM focuses on book title
  - Use insert_book_mention() (not insert_book_mention_with_sentiment()) when not storing sentiment
  - Always clear old data when doing fresh extraction to avoid stale/duplicate entries
---

## 2026-01-20 - US-021
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented parallel book extraction with configurable workers
- Files modified:
  - main.py: Added --workers flag to analyze command (default 5)
  - analyzer.py: Modified extract_books_from_posts_llm() to use ThreadPoolExecutor
- Features:
  - ThreadPoolExecutor for parallel LLM calls (same pattern as scraping and ranking)
  - RateLimiter class controls request rate across workers (default 5 requests/second)
  - Results collected from workers, written to DB sequentially in batches of 50
  - Connection errors trigger graceful shutdown of remaining workers
  - Progress logging shows posts processed and books found (every 50 posts)
  - Handles individual post failures gracefully (logs warning, marks processed, continues)
  - Posts marked as processed even on error to avoid re-processing on next run
  - Command: python main.py analyze --use-ollama --extract-only --workers 10
- **Learnings for future iterations:**
  - Same parallel pattern used in US-012, US-016, and now US-021: ThreadPoolExecutor + RateLimiter + sequential DB writes
  - Batch DB writes (50 at a time) balances memory usage with write efficiency
  - Use `as_completed()` to process results as they arrive, not in submission order
  - Cancel remaining futures on connection error to fail fast
  - Mark posts as processed even on error to avoid infinite retry loops
---

## 2026-01-20 - US-022
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Improved JSON parse error logging when LLM responses fail to parse
- Files modified:
  - analyzer.py: Updated analyze_post_with_llm() and extract_books_only()
- Features:
  - Added post_title parameter to analyze_post_with_llm() for error logging
  - JSON parse errors now include post title to identify which post failed
  - Raw LLM response logged (truncated to 500 chars to avoid log spam)
  - Updated call site in analyze_posts_full_context() to pass title
  - Log format: "Warning: Could not parse JSON from LLM response for post: {title}:\n{truncated_response}"
- **Learnings for future iterations:**
  - When adding diagnostic logging, include enough context to identify the source (e.g., post title)
  - Truncate long outputs to avoid overwhelming logs - 500 chars is a good balance
  - When adding parameters to existing functions, make them optional with default None for backwards compatibility
---

## 2026-01-20 - US-023
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Fixed JSON parsing issues and improved book title extraction prompt
- Files modified:
  - analyzer.py: Added clean_markdown_from_json(), extract_titles_from_malformed_response(), updated extract_books_only() prompts and parsing
- Features:
  - clean_markdown_from_json(): Strips markdown (* and _) from outside quoted strings using state machine approach
  - extract_titles_from_malformed_response(): Fallback regex extraction for quoted strings and numbered/bulleted lists
  - Updated prompts with explicit no-markdown instruction
  - Updated prompts with full title+subtitle handling instructions
  - Parsing logic now: (1) cleans markdown, (2) tries JSON parse, (3) falls back to regex extraction
  - Shows "Note: Used fallback extraction" when regex fallback succeeds
- **Learnings for future iterations:**
  - State machine approach (tracking in_quotes flag) is robust for parsing chars inside/outside strings
  - When cleaning JSON, preserve content inside quotes while stripping markdown outside
  - Fallback extraction with multiple patterns (quoted strings, numbered lists, bulleted lists) increases success rate
  - Prompts should be explicit about format requirements - "Return ONLY valid JSON. Do NOT use markdown."
  - For title+subtitle: instruct LLM to combine if mentioned separately (common in blog posts)
---

## 2026-01-20 - US-024
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Made extraction incremental by default (skips already-extracted posts)
- Added --reset flag to explicitly clear data and reprocess all posts
- Fixed error handling to NOT mark posts as extracted on errors (enables retry)
- Added logging for skipped vs processed post counts at start
- Files modified:
  - main.py: Added --reset flag to analyze command, updated help text
  - analyzer.py: Modified analyze_all_posts() with reset parameter, updated extract_only logic, fixed error handling in extract_books_from_posts_llm()
  - db.py: Added get_extraction_counts() function for extraction statistics
- **Learnings for future iterations:**
  - For incremental processing, separate "successful extraction with 0 results" from "failed extraction"
  - Success (even 0 books) = mark as processed; Error = leave unprocessed for retry
  - get_extraction_counts() returns (total_with_content, already_extracted, to_process) for logging
  - --reset flag should only clear data when explicitly requested, not by default
---
